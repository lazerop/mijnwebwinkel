'use strict';

(function() {
    angular
        .module('webcart', ['ngCookies'])
        .run(function ($rootScope) {
        })

        .config(function () {
        })

        .value('restLocation', 'http://localhost:8080')
})();
